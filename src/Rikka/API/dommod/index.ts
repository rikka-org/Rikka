// Dom mod that allows sandboxed code to access the DOM, while also preventing access to sensitive elements.

import BaseAPI from "@rikka/API/BaseAPI";

export default class dommod extends BaseAPI {
}
